const Rendering = ( {a} ) => {
    const b = null;
    const c = '';
    const d = 0;
    return <div>
        <div>{a}</div>
        <div>{b}</div>
        <div>{c}</div>
        <div>{d}</div>
    </div>
}

export default Rendering;